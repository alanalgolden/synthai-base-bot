/* import { addStringToVectorDb } from "./addStringToVectorDb/route";

export default async function handler(req, res) {
  if (req.method === "POST") {
    const pinecone = await addStringToVectorDb();
  } else {
    res.status(405).json({ message: "Method not allowed" });
  }
}
 */
